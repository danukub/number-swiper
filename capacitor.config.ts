import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'number-swiper',
  webDir: 'dist/number-swiper',
  bundledWebRuntime: false
};

export default config;
